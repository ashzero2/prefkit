{
  "name": "@prefkit/cli",
  "version": "0.1.0",
  "description": "Local-first preference memory and context injection for coding agents.",
  "type": "module",
  "files": [
    "dist"
  ],
  "bin": {
    "prefkit": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@prefkit/core": "workspace:^"
  },
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  }
}
